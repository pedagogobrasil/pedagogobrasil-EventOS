# API

EventOS is planned as an API-first platform.

The first public web interface will use server-rendered PHP routes. As modules mature, application services will be exposed through authenticated JSON endpoints.

Future API goals:

- Mobile applications.
- Public display panels.
- PIX integrations.
- Reporting integrations.
- AI-assisted administrative queries.
